version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10-browsers

    steps:
      - checkout

      - run:
        name: コードの取得
        command: |
          ls -al
          echo '^^^That should look familiar^^^'
      - run:
        name: 独自コンテナで実行
        command: |
          node -
